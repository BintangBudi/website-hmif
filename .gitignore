# Laravel Framework
/bootstrap/cache/*
!.bootstrap/cache/.gitignore
/storage/app/*
/storage/framework/*
/storage/logs/*
!.storage/app/.gitignore
!.storage/app/public/.gitignore
!.storage/framework/.gitignore
!.storage/framework/cache/.gitignore
!.storage/framework/sessions/.gitignore
!.storage/framework/testing/.gitignore
!.storage/framework/views/.gitignore
!.storage/logs/.gitignore
/public/storage
/public/hot
/.phpunit.result.cache
/phpunit.xml # Often customized per environment, .env controls test db
Homestead.yaml
Homestead.json
composer.phar

# Environment files - IMPORTANT: Never commit your actual .env file!
.env
.env.*
!.env.example
!.env.testing

# PHP Composer dependencies
/vendor/

# Node.js dependencies
/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log

# IDE & Editor specific files
.idea/
.vscode/
*.sublime-project
*.sublime-workspace
*.komodoedit/
*.project
.cproject
.settings/
*.tmproj
nbproject/ # NetBeans

# Operating System Files
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Log files (general, in addition to Laravel's storage/logs)
*.log

# Build/Distribution Folders (if you have custom build steps)
build/
dist/
public/build/ # For Laravel Mix or Vite build assets (if not using public/hot)

# Temporary files
*.tmp
*.temp
*.swp
*.swo

# Uploads - If you store user uploads directly in public, ignore them
# public/uploads/

# Miscellaneous
*.zip
*.tar.gz
*.rar
*.pid
*.seed

# Sass Specific
.sass-cache/
*.css.map

# Vagrant
.vagrant/

# SQLite databases (if used for local development and not part of the repo)
*.sqlite
*.sqlite3

# PHPStan / Psalm (Static Analysis Tools)
phpstan-baseline.neon
phpstan.neon.dist # if it contains local paths
psalm.xml.dist # if it contains local paths
.psalm/cache/
